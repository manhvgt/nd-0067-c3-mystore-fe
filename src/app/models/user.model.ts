export interface UserInformation {
  name: string;
  phoneNumber: string;
  email: string;
  cardNumber: string;
  cardName: string;
  validationDate: string;
  securityCode: string;
  address: string;
  userNote: string;
}
